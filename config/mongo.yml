defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: dnd1_development

test:
  <<: *defaults
  database: dnd1_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: dnd1
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
